spring:
  application:
    name: reggie_take_out
  redis:
    host: localhost
    port: 6379
    database: 0
    jedis:
      pool:
        max-active: 8
        max-wait: 1ms
        max-idle: 4
        min-idle: 0
  datasource:
    druid:
      username: root
      password: 123456
      url: jdbc:mysql://localhost:3306/reggie
      driver-class-name: com.mysql.jdbc.Driver
  cache:
    redis:
      time-to-live: 18000000

mybatis-plus:
  config-location:
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      id-type: assign_id
reggie:
  path: /Users/cherish/Downloads/hh/

